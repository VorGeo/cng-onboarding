// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-dockerfile
{
	"name": "Guide: Reading Landsat from MSPC",
	"dockerComposeFile": ["../../docker-compose.yml", "docker-compose.codespaces.yml"],
	"service": "guide-stac-mspc-landsat-service",
  "shutdownAction": "none",
  "workspaceFolder": "/workspace/tutorials/stac/mspc_stac",

	// Make the container name and service name available to the container.
	"containerEnv": {
		"SERVICE_NAME": "guide-stac-mspc-landsat-service",
		"CONTAINER_NAME": "Guide: Reading Landsat from MSPC"
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Install pixi, use it to install dependencies, and create a kernel for Jupyter.
	"postCreateCommand": "/workspace/.devcontainer/postCreateCommand.sh",

	// Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
				"ms-python.python",
				"ms-toolsai.jupyter"
			],
    }
  }

	// Uncomment to connect as an existing user other than the container default. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "devcontainer"
}
